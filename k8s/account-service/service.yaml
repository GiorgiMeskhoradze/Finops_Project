apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: finops
spec:
  selector:
    app: account-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP