apiVersion: v1
kind: Service
metadata:
  name: transaction-service
  namespace: finops
spec:
  selector:
    app: transaction-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP